# 为 Jiaojie 贡献

感谢你帮助 AI 在会话、模型、设备和语言之间保持工作连续性。

## 最适合提交的贡献

- 能稳定复现的连续性失败案例；
- 新模型、语言或 Runtime 的完整证据包；
- 协议、Schema、转换器、安全扫描或文档修复；
- 新领域案例与独立盲评；
- 对 Jiaojie 格式的第三方实现。

不要提交真实密钥、私人会话、未经授权的数据或只有结论没有原始记录的“测试通过”声明。

## 开始

1. 先搜索现有 Issue，避免重复工作；
2. 对协议、Schema 或兼容性声明的变化，先开 Proposal Issue；
3. Fork 仓库并从 `main` 创建小而聚焦的分支；
4. 修改后运行 `python3 scripts/project_check.py`；
5. PR 中填写复现步骤、实际结果、限制和安全检查项。

## 证据贡献契约

模型或 Runtime 结果必须包含：

- 精确的模型或 Runtime 名称、版本与日期；
- Producer 与 Receiver 的角色和语言方向；
- 输入、交接产物、原始响应、评分与失败项；
- 工具、文件、网络和上下文能力；
- 是否人工复核，复核者与生成 Agent 必须分离；
- 所有敏感值已删除的说明。

失败结果和成功结果使用相同格式。不得删去失败运行后只报告最佳一次。

## 案例与许可

新案例默认使用合成数据。若包含第三方材料，必须注明来源、许可和可公开范围。个人数据、客户材料和未公开代码不得进入仓库。案例文件应显式标记 `synthetic`、`license`、`status` 和 `limitations`。

## 代码与文档

- Python 工具保持标准库优先，兼容 Python 3.10+；
- 错误码、Schema 字段和协议 ID 是兼容接口，变更需测试；
- `SKILL.md` 保持简短，只放触发与核心流程，细节进入 `references/`；
- README 的 ✅ 必须能链接到仓库内可复现证据；
- 用户可见文本可以多语言，协议标识符不得随意翻译。

## 提交与 PR

建议使用清晰的提交前缀：`feat:`、`fix:`、`docs:`、`test:`、`security:`、`eval:`、`chore:`。

提交 PR 即表示你有权贡献这些内容，并同意按仓库的 MIT License 发布代码和文档。测试数据中单独声明的许可证优先适用于该数据。

## 行为与安全

参与即同意 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。安全漏洞请遵循 [`SECURITY.md`](SECURITY.md)，不要在公开 Issue 中披露可利用细节。
